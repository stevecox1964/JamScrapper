# Handoff — Player card and history panel auto-hide with pull-tabs

**Date:** 2026-09-18 17:28
**Branch:** main
**Prior handoff:** [HANDOFF_2026-09-16_1639.md](HANDOFF_2026-09-16_1639.md)

## TL;DR

The bottom player card and the play history panel now pop out on each new song and slide
away after 6 seconds, leaving a pull-tab (▲ bottom, ▶ left edge). Build and lint pass; the
user has not confirmed the history panel live yet. Next big work is unchanged: rewire
`backend/radio.py` scoring to mood distance.

## What was done this session

- Bottom player card pull-tab made bigger and brighter (80×36 tab, 18px arrow) —
  `frontend/src/App.css` `.player-controls-tab`. The hide feature already existed; the user
  could not find the old 56×28 tab with its 12px dim arrow. Commit `609fdf7`.
- Player card auto-hide: new song → card shows → slides down after `AUTO_HIDE_SECONDS = 6`
  — `frontend/src/components/PlayerControls.jsx`. Song key = `artist|title`. A tab click
  cancels the pending timer. Commit `4ed7412`. User said "Fantastic".
- History panel auto-hide, same behavior, slides left — `frontend/src/components/SongHistory.jsx`.
  New `.history-dock` wrapper owns `position: fixed` + transform (the panel scrolls, so a tab
  inside it would be clipped). Song key = newest history entry `artist|title|timestamp`, so a
  replay also pops it out. Commit `0293e9a`.
- README: new paragraph under "How It Works", a history panel bullet, and project structure
  lines updated. Commit `72ee869`.

## In flight

_None._

## Decisions locked in this session

- Each new song brings the cards back, even after a manual hide. User heard the option
  "your hide stays" and did not pick it (said "Hold", then moved on).
- 6 seconds auto-hide for both cards, same as the constant name in each file.
- "Reset on new song" uses the render-time `shownKey` state pattern, not `setState` inside
  `useEffect` — the repo's eslint `react-hooks/set-state-in-effect` rule rejects the latter.

## Open questions

- In capture mode the save color (green = safe to skip) is not visible while the player card
  is down. Offered: also color the ▲ tab. User has not answered.
- Carried over: (3) "✓ Saved" word on the card; (4) ~30 font sizes under 12px in `App.css`.
- Carried over: blend or replace genre overlap in `radio.py` with mood distance, and what Next
  should push toward.

## Next concrete step

Read `backend/radio.py::_taste()` and the pick scoring around it, then add a
`MoodStore.all_moods()` read and score candidates by distance to a mood target built from the
last few finished songs and pushed away from the last Next'd song. Test with `python -c`
against the real DB before touching `/radio/next`.

## Files touched this session

- `frontend/src/App.css` — bigger player tab; new `.history-dock`, `.history-dock-tab` styles;
  `.history-panel` lost its fixed positioning (moved to the dock).
- `frontend/src/components/PlayerControls.jsx` — auto-hide timer + reset on new song.
- `frontend/src/components/SongHistory.jsx` — dock wrapper, ▶/◀ tab, auto-hide timer.
- `README.md` — documents the auto-hide cards.
- `plan/handoffs/HANDOFF_2026-09-18_1728.md`, `plan/handoffs/LATEST.md` — this handoff.

## How to resume

Paste this prompt at the start of the next session:

> Resume the previous session. Read `plan/handoffs/LATEST.md` for full context. The next concrete step is: rewire `backend/radio.py` pick scoring to mood distance using `MoodStore.all_moods()`, tested with `python -c` against the real DB first.

## Git state at handoff

```
(clean before this handoff file)
```

Recent commits:

```
72ee869 README: auto-hiding player card and history panel with pull-tabs
0293e9a Play history panel pops out on each new song, then slides left after 6 seconds
4ed7412 Player card pops up on each new song, then slides down after 6 seconds
609fdf7 Make the player card pull-tab bigger and brighter so it is easy to find
f86d63e Handoff 2026-09-16: capture card save colors, README section
```

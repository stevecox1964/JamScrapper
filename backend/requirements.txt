soundcard>=0.4.6
websockets>=14.0
numpy>=2.0.0
winrt-Windows.Media.Control>=3.2.1
winrt-Windows.Storage.Streams>=3.2.1
# Not pulled in automatically, but the media session needs both: Foundation for
# the async calls, Collections for get_sessions(). Missing either one makes the
# WinRT detector fail at runtime and fall back to title scraping.
winrt-Windows.Foundation>=3.2.1
winrt-Windows.Foundation.Collections>=3.2.1
requests>=2.28.0
Pillow>=12.0.0
pyacoustid>=1.3.1
yt-dlp>=2024.0.0

# Song signatures - measured audio features from the local mp4 files.
# Needs ffmpeg on PATH too; it extracts the audio librosa then analyses.
# librosa pulls in numba + llvmlite, which are compiled: on a new Python
# release these are the first things to lack wheels. Verified on 3.14.
librosa>=1.0.0
soundfile>=0.13.0

# Secrets come from the git-ignored .env files (see env.py).
python-dotenv>=1.0.0

# Mood labels - Claude reads artist/title/genres and returns mood words.
anthropic>=1.0.0
